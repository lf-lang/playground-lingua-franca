#ifndef TYPES_HPP
#define TYPES_HPP

typedef enum LockCommand {LOCK, UNLOCK} LockCommand;

typedef enum OpenEvent {OPEN, CLOSE} OpenEvent;

typedef enum LockEvent {LOCKED, UNLOCKED} LockEvent;

#endif
